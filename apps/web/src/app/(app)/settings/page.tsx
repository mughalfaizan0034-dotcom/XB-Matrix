import { ModulePlaceholder } from '@/components/module-placeholder';

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Organization, workspaces, users, permissions, and integrations."
    />
  );
}
