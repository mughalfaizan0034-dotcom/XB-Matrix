name: deploy-web

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/ui/**'
      - 'packages/types/**'
      - 'packages/config/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build static export
        env:
          WEB_OUTPUT: export
          # MUST match the GitHub Pages path exactly (case-sensitive).
          # Repo is `XB-Matrix`, so Pages serves from /XB-Matrix/. Setting
          # this wrong breaks all asset URLs (CSS/JS 404, unstyled HTML).
          WEB_BASE_PATH: /XB-Matrix
          NEXT_PUBLIC_API_BASE_URL: ${{ vars.NEXT_PUBLIC_API_BASE_URL }}
          NEXT_PUBLIC_APP_NAME: xB Matrix
        # `@xb/web...` (three dots) builds @xb/web's workspace dependencies
        # first (types, config, auth) so Next.js can resolve their dist/ output.
        # @xb/ui keeps src/ exports and is handled by Next's transpilePackages.
        run: pnpm --filter @xb/web... build

      - name: Add .nojekyll
        run: touch apps/web/out/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
